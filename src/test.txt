css
256 - justify-content: flex-start;

jsx
84 - const [spin, setSpin] = useState(false);

after 229 - 
    setSpin(true);
    setTimeout(() => {
      setSpin(false);
      setRunEnable(true);
    }, 3000);


after 412 - 
    defaultValue={"chat-gpt4"}
    disabled={true}
    
fasdf

after 424 - 
    defaultValue={"presidio"}

452 - {spin ? (
              <Spin
                indicator={
                  <LoadingOutlined
                    style={{
                      fontSize: 25,
                      color: "white",
                    }}
                  />
                }
                spinning={spin}
              />
            ) : (
              <p>Run</p>
            )}

452 - disabled={!run_enable}